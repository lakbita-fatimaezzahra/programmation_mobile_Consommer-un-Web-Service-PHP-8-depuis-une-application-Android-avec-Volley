<?xml version="1.0" encoding="utf-8"?>
<resources>


        <!-- Style pour les labels (TextView) -->
        <style name="labelText">
            <item name="android:textSize">16sp</item>
            <item name="android:textColor">@android:color/holo_blue_dark</item>
            <item name="android:paddingBottom">8dp</item>è
            <item name="android:layout_width">match_parent</item>   <!-- ← OBLIGATOIRE -->
            <item name="android:layout_height">wrap_content</item>  <!-- ← OBLIGATOIRE -->
            <item name="android:layout_marginBottom">8dp</item>
        </style>

        <!-- Style pour les champs de saisie (EditText, Spinner) -->
        <style name="inputField">
            <item name="android:padding">10dp</item>
            <item name="android:background">@android:drawable/edit_text</item>
            <item name="android:textColor">@android:color/black</item>
            <item name="android:textSize">15sp</item>
            <item name="android:layout_width">match_parent</item>   <!-- ← OBLIGATOIRE -->
            <item name="android:layout_height">wrap_content</item>  <!-- ← OBLIGATOIRE -->
            <item name="android:layout_marginBottom">8dp</item>
        </style>

        <!-- Style pour le bouton -->
        <style name="actionButton">
            <item name="android:layout_width">match_parent</item>
            <item name="android:layout_height">wrap_content</item>
            <item name="android:backgroundTint">@android:color/holo_green_dark</item>
            <item name="android:textColor">@android:color/white</item>
            <item name="android:padding">12dp</item>
            <item name="android:layout_marginBottom">8dp</item>
        </style>

    </resources>
